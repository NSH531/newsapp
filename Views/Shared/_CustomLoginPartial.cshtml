@using Microsoft.AspNet.Identity
<script>
    function a() {
        sessionStorage.removeItem('accessToken');
    }
</script>
@{if(User.Identity.IsAuthenticated){
      <ul>
          <li>@Html.ActionLink("Hello " + User.Identity.GetUserName() + "!", "Index", "Manage", routeValues: new { area = "" }, htmlAttributes: new { title = "Manage" })
        </li>
          
        <li>@using (Html.BeginForm("LogOff2", "Account", FormMethod.Post))
        {
            
                @Html.Hidden("v1",User.Identity.AuthenticationType)
                <input type="submit" value=" log off"><span class="glyphicon glyphicon-log-out"></span>
            
        }
           
                
            </li>
    </ul>
    
}
else
{

    <ul class="nav navbar-nav navbar-right">
        <li>@Html.ActionLink("Register", "Register", "Account", routeValues: new { area = "" }, htmlAttributes: new { id = "registerLink" })</li>
        <li>@Html.ActionLink("Log in", "Login", "Account", routeValues: new { area = "" }, htmlAttributes: new { id = "loginLink" })</li>
    </ul>
}


}